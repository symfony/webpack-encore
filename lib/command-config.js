/**
 * Tracks current configuration for a build.
 *
 * This is overridden in bin/encore and read ultimately
 * by index.js to configure things.
 */
module.exports = {
    environment: null,
    useDevServer: null,
    devServerUrl: null
};
